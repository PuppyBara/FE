import 'package:flutter/material.dart';

class ProtectingScreen extends StatelessWidget {
  const ProtectingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
